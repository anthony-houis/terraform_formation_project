OpenTofu infrastructure project for AWS

To run properly:
In order to setup a shared tfstat lock system,
run "tofu init" into Backend directory, then plan and apply.
Then, init, plan and apply into the vpc folder.
